<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projects</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .rounded-box {
      border-radius: 15px;
      padding: 20px;
      background-color: #ffffff;
      width: 250px;
      height: 175px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body>

  <p id="notice"><%= notice %></p>

  <h1>Calendar</h1>
  <% @projects.each do |project| %>
    <%= link_to project do %>
      <div class="rounded-box my-3" style="text-decoration: none; color: inherit;">
        <h3><%= project.title %></h3>
        <p><%= project.description %></p>
        
        <p>Start: <%= project.start_date.strftime('%Y-%m-%d %H:%M') if project.start_date %></p>
        <p>End: <%= project.end_date.strftime('%Y-%m-%d %H:%M') if project.end_date %></p>
      </div>
    <% end %>
  <% end %>
  
  <br>
  
  <%= link_to 'New event', new_project_path, class: "btn btn-outline-success" %>

</body>
</html>
