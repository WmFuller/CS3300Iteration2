<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Projects</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .rounded-box {
      border-radius: 15px;
      padding: 20px;
      background-color: #ffffff;
      width: 250px;
      height: 175px;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>

<body>
<% if user_signed_in? %>
  <p id="notice"><%= notice %></p>

  <h1>Calendar</h1>
  
  <% date = Date.today.beginning_of_month %>
<% last_day = date.end_of_month %>
<% prev_month = date.prev_month %>
<% next_month = date.next_month %>

<h1><%= date.strftime("%B %Y") %></h1>
</table>  
  <br>
  <%= link_to 'New event', new_project_path, class: "btn btn-outline-success" %>
</body>
<table>
  <thead>
    <tr>
      <% Date::ABBR_DAYNAMES.each do |day| %>
        <th><%= day %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% while date <= last_day %>
      <tr>
        <% 7.times do %>
          <td>
            <% if date.month == prev_month.month || date.month == next_month.month %>
              <%= "" %>
            <% else %>
              <%= date.day %>
              <br>
              <% @projects.each do |project| %>
                <% if project.start_date.to_date == date %>
                  <%= link_to project do %>
                    <div class="rounded-box my-3" style="text-decoration: none; color: inherit;">
                      <h3><%= project.title %></h3>
                      <p><%= project.description %></p>
                      
                      <p>Start: <%= project.start_date.strftime('%m-%d-%Y %H:%M') if project.start_date %></p>
                      <p>End: <%= project.end_date.strftime('%m-%d-%Y %H:%M') if project.end_date %></p>
                    </div>
                  <% end %>
                <% end %>
              <% end %>
            <% end %>
          </td>
          <% date = date.next_day %>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</body>
<% end %>
</html>
